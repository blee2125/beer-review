<h1>Comments</h1>

<a class="button" href="<%= new_beer_comment_path(@beer) %>">New Comment</a>



    <div class="box">
        <% @beer.comments.each do |comment| %>
            <div class="box" id="commentbox">
                <p><a href="<%= user_path(comment[:user_id]) %>"><%= comment.user.username %></a> - <%= comment.comment %></p><br>
            </div>
        <% end %>
    </div>

