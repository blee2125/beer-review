<%= form_for @comment, url: beer_comments_path  do |f| %>

    <%= f.hidden_field :beer_id, value: params[:beer_id] %>
    <%= f.hidden_field :user_id, value: current_user.id %>

    <%= f.label :comment, "Comment" %>
    <%= f.text_area :comment %><br>

    <%= f.submit %>

<% end %>